//  node js
require("./函数式适配器")

let 数 = 1

假如(数 > 1)
    .则(() => {
        打印("大于一")
    })
    .否则(() => {
        打印("不大于一")
    })

程序()
    .令("人数", 13)
    .令("费用", { 餐饮: 227, 住宿: 600 })
    .令("出发.日期", "2022/09/05")
    .执行((状态, 程序) => {
        打印(状态.人数)
        打印(状态.出发.日期)
        打印(状态.费用.住宿)
        打印(程序)
    })
    .执行(`状态.人数 += 1`)
    .执行(状态 => {
        打印(状态.人数)
    })
    .令('人数', 200)
    .如果(`状态.人数 > 100`)
    .那么(`打印('大于100')`)
    .否则(`打印('不大于100')`)
    .执行(`打印(状态.人数)`)
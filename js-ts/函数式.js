//  node js
require("./函数式适配器")

let 数 = 1

假如(数 > 1).则(() => {
    打印("大于一")
}).否则(() => {
    打印("不大于一")
})


状态({ 人数: 13, 费用: { 餐饮: 227, 住宿: 600 } }).执行(状态 => {
    打印(状态.人数)
    打印(状态.费用.住宿)
}).执行(函数(`状态.人数 += 1`)).执行(状态 => {
    打印(状态.人数)
})